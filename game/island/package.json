{
  "name": "island-pixel-starter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Phaser 3 + Vite + TypeScript 的俯視角像素遊戲起手式",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "assets": "node tools/gen-tileset.mjs && node tools/gen-characters.mjs && node tools/gen-map.mjs",
    "standalone": "npm run build && node tools/build-standalone.mjs"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^7.3.6"
  }
}
